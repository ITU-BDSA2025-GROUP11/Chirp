@using Microsoft.AspNetCore.Identity
@using Chirp.Core.DomainModel
@inject SignInManager<Author> SignInManager
@inject UserManager<Author> UserManager

<div class="navigation">
    <!-- LOGIN PARTIAL LOADED -->
    @if (SignInManager.IsSignedIn(User))
    {
        <div>
            <a class="nav-link text-dark" asp-area="Identity"
               asp-page="/User">My Timeline</a> |
            <a class="nav-link text-dark" asp-area="Identity"
               asp-page="/Public">Public Timeline</a> |
            <a class="nav-link text-dark" asp-area="Identity"
                asp-page="/Public">Logout [@(User.Identity.Name)]</a>
        </div>
    }
    else
    {
        <div>
            <a class="nav-link text-dark" asp-area="Identity"
               asp-page="/Account/Register">Register</a> |
            <a class="nav-link text-dark" asp-area="Identity"
               asp-page="/Account/Login">Login</a> 
        </div>
    }
</div>

<!-- 
< class="nav-item">
<a class="nav-link text-dark" asp-area="Identity"
   asp-page="/Account/Manage/Index" title="Manage">
    Hello @User.Identity.Name!>
</a>
<
<div class="nav-item">
    <form class="form-inline" asp-page="/Account/Logout"
          asp-route-returnUrl="@Url.Page("/", new { area = "" })"
          asp-area="Identity" method="post">
        <button class="nav-link btn btn-link text-dark"
                type="submit">Logout</button>
    </form>
</div>
}
else
{
<div class="nav-item">
    <a class="nav-link text-dark" asp-area="Identity"
       asp-page="/Account/Register">Register</a>
    <a class="nav-link text-dark" asp-area="Identity"
       asp-page="/Account/Login">Login</a>
</div>
<div class="nav-item">
   
</div>

-->